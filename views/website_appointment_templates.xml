<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="appointment_form" name="Agenda tu cita">
        <t t-call="website.layout">
            <div id="wrap" class="oe_structure">
                <section class="container py-5 optica-appointment-page">
                    <div class="row justify-content-center">
                        <div class="col-lg-8 optica-appointment-card">
                            <h1 class="mb-3">Agenda tu cita</h1>
                            <p class="lead mb-4">
                                Completa tus datos y nuestro equipo confirmará tu atención en la óptica.
                            </p>

                            <t t-if="errors.get('general')">
                                <div class="alert alert-danger" role="alert">
                                    <t t-esc="errors.get('general')"/>
                                </div>
                            </t>

                            <form action="/agendar-cita" method="post" class="o_website_form">
                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                                <div class="mb-3">
                                    <label for="patient_name" class="form-label">Nombre</label>
                                    <input
                                        type="text"
                                        name="patient_name"
                                        id="patient_name"
                                        class="form-control"
                                        required="required"
                                        t-att-value="form_values.get('patient_name', '')"
                                    />
                                    <t t-if="errors.get('patient_name')">
                                        <small class="text-danger">
                                            <t t-esc="errors.get('patient_name')"/>
                                        </small>
                                    </t>
                                </div>
                        

                               <!-- <div class="mb-3">
                                    <label for="phone" class="form-label">Teléfono</label>
                                    <input
                                        type="tel"
                                        name="phone"
                                        id="phone"
                                        class="form-control"
                                        required="required"
                                        t-att-value="form_values.get('phone', '')"
                                    />
                                    <t t-if="errors.get('phone')">
                                        <small class="text-danger">
                                            <t t-esc="errors.get('phone')"/>
                                        </small>
                                    </t>
                                </div> -->

                                <div class="mb-3">
                                    <label for="whatsapp" class="form-label">WhatsApp</label>
                                    <input
                                        type="tel"
                                        name="whatsapp"
                                        id="whatsapp"
                                        class="form-control"
                                        required="required"
                                        t-att-value="form_values.get('whatsapp', '')"
                                    />
                                </div>

                                <div class="mb-3">
                                    <label for="email" class="form-label">Email</label>
                                    <input
                                        type="email"
                                        name="email"
                                        id="email"
                                        class="form-control"
                                        required="required"
                                        t-att-value="form_values.get('email', '')"
                                    />
                                    <t t-if="errors.get('email')">
                                        <small class="text-danger">
                                            <t t-esc="errors.get('email')"/>
                                        </small>
                                    </t>
                                </div>

                                 <!--  <div class="mb-3">
                                    <label for="appointment_type" class="form-label">Tipo de cita</label>
                                    <select
                                        name="appointment_type"
                                        id="appointment_type"
                                        class="form-control"
                                        required="required"
                                    >
                                        <option value="exam" t-att-selected="'selected' if form_values.get('appointment_type', 'exam') == 'exam' else None">
                                            Examen visual
                                        </option>
                                        <option value="delivery" t-att-selected="'selected' if form_values.get('appointment_type') == 'delivery' else None">
                                            Entrega de lentes
                                        </option>
                                        <option value="adjustment" t-att-selected="'selected' if form_values.get('appointment_type') == 'adjustment' else None">
                                            Ajuste de armazón
                                        </option>
                                        <option value="warranty" t-att-selected="'selected' if form_values.get('appointment_type') == 'warranty' else None">
                                            Garantía
                                        </option>
                                        <option value="progressive_adaptation" t-att-selected="'selected' if form_values.get('appointment_type') == 'progressive_adaptation' else None">
                                            Adaptación de progresivos
                                        </option>
                                        <option value="other" t-att-selected="'selected' if form_values.get('appointment_type') == 'other' else None">
                                            Otro
                                        </option>
                                    </select>
                                    <t t-if="errors.get('appointment_type')">
                                        <small class="text-danger">
                                            <t t-esc="errors.get('appointment_type')"/>
                                        </small>
                                    </t>
                                </div> -->

                             <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <label for="appointment_date" class="form-label">Fecha deseada</label>
                                        <input
                                            type="date"
                                            name="appointment_date"
                                            id="appointment_date"
                                            class="form-control"
                                            required="required"
                                            t-att-value="form_values.get('appointment_date', '')"
                                        />
                                        <t t-if="errors.get('appointment_date')">
                                            <small class="text-danger">
                                                <t t-esc="errors.get('appointment_date')"/>
                                            </small>
                                        </t>
                                    </div>

                               <div class="col-md-6 mb-3">
                                <label for="appointment_time" class="form-label">Hora deseada</label>

                                <select
                                    name="appointment_time"
                                    id="appointment_time"
                                    class="form-control"
                                    required="required"
                                >
                                <option value="">Selecciona una hora</option>

                                <option value="10:00" t-att-selected="'selected' if form_values.get('appointment_time') == '10:00' else None">10:00</option>
                                <option value="10:30" t-att-selected="'selected' if form_values.get('appointment_time') == '10:30' else None">10:30</option>
                                <option value="11:00" t-att-selected="'selected' if form_values.get('appointment_time') == '11:00' else None">11:00</option>
                                <option value="11:30" t-att-selected="'selected' if form_values.get('appointment_time') == '11:30' else None">11:30</option>

                                <option value="12:00" t-att-selected="'selected' if form_values.get('appointment_time') == '12:00' else None">12:00</option>
                                <option value="12:30" t-att-selected="'selected' if form_values.get('appointment_time') == '12:30' else None">12:30</option>
                                <option value="13:00" t-att-selected="'selected' if form_values.get('appointment_time') == '13:00' else None">13:00</option>
                                <option value="13:30" t-att-selected="'selected' if form_values.get('appointment_time') == '13:30' else None">13:30</option>

                                <option value="14:00" t-att-selected="'selected' if form_values.get('appointment_time') == '14:00' else None">14:00</option>
                                <option value="14:30" t-att-selected="'selected' if form_values.get('appointment_time') == '14:30' else None">14:30</option>
                                <option value="15:00" t-att-selected="'selected' if form_values.get('appointment_time') == '15:00' else None">15:00</option>
                                <option value="15:30" t-att-selected="'selected' if form_values.get('appointment_time') == '15:30' else None">15:30</option>

                                <option value="16:00" t-att-selected="'selected' if form_values.get('appointment_time') == '16:00' else None">16:00</option>
                                <option value="16:30" t-att-selected="'selected' if form_values.get('appointment_time') == '16:30' else None">16:30</option>
                                <option value="17:00" t-att-selected="'selected' if form_values.get('appointment_time') == '17:00' else None">17:00</option>
                                <option value="17:30" t-att-selected="'selected' if form_values.get('appointment_time') == '17:30' else None">17:30</option>

                                <option value="18:00" t-att-selected="'selected' if form_values.get('appointment_time') == '18:00' else None">18:00</option>
                                <option value="18:30" t-att-selected="'selected' if form_values.get('appointment_time') == '18:30' else None">18:30</option>
                            </select>

                            <t t-if="errors.get('appointment_time')">
                                <small class="text-danger">
                                        <t t-esc="errors.get('appointment_time')"/>
                                </small>
                            </t>
                        </div>
                             </div>
                               <!-- <div class="mb-3">
                                    <label for="reason" class="form-label">Motivo de la cita</label>
                                    <textarea
                                        name="reason"
                                        id="reason"
                                        class="form-control"
                                        rows="4"
                                        required="required"
                                    ><t t-esc="form_values.get('reason', '')"/></textarea>
                                    <t t-if="errors.get('reason')">
                                        <small class="text-danger">
                                            <t t-esc="errors.get('reason')"/>
                                        </small>
                                    </t>
                                </div> -->

                                <button type="submit" class="btn btn-primary">
                                    Agendar cita
                                </button>
                            </form>
                        </div>
                    </div>
                </section>
            </div>
        </t>
    </template>

    <template id="appointment_success" name="Cita registrada">
        <t t-call="website.layout">
            <div id="wrap" class="oe_structure">
                <section class="container py-5 text-center">
                    <h1 class="mb-3">¡Gracias!</h1>
                    <p class="lead">
                        Tu cita fue registrada correctamente. Te contactaremos para confirmar cualquier detalle adicional.
                    </p>
                    <a href="/" class="btn btn-primary mt-3">
                        Volver al inicio
                    </a>
                </section>
            </div>
        </t>
    </template>

</odoo>
